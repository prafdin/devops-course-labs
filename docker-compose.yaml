services:
  backend:
    image: ghcr.io/getsomelight/catty-reminders-app:${DEPLOY_REF:-latest}
    ports:
      - "8181:8181"
    restart: always
    depends_on:
      - db

  db:
    image: mariadb:10.11
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: mypass
      MARIADB_DATABASE: catty_reminders
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: