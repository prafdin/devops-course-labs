services:
  app:
    image: ${IMAGE:-ghcr.io/vadimkadkdoofjf/catty-reminders-app:latest}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8181:8181"
    env_file:
      - .env
    environment:
      - DEPLOY_REF=${DEPLOY_REF:-latest}
      - MYSQL_HOST=db
      - MYSQL_USER=catty
      - MYSQL_PASSWORD=cattypass
      - MYSQL_DATABASE=catty_reminders
    volumes:
      - ./config.json:/catty-reminders-app/config.json:ro
    networks:
      - app-network

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_USER: catty
      MYSQL_PASSWORD: cattypass
      MYSQL_DATABASE: catty_reminders
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -ucatty -pcattypass"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 20s
    volumes:
      # Указываем точный путь к папке внутри проекта
      - /home/vboxuser/catty-reminders-app/mysql_data:/var/lib/mysql
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
