services:
  app:
    image: ${IMAGE:-ghcr.io/vadimkadkdoofjf/catty-reminders-app:latest}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8181:8181"
    env_file:
      - .env
    environment:
      - DEPLOY_REF=${DEPLOY_REF:-latest}
      - MYSQL_HOST=db
      - MYSQL_USER=catty
      - MYSQL_PASSWORD=cattypass
      - MYSQL_DATABASE=catty_reminders
    volumes:
      # Монтируем конфиг только на чтение
      - ./config.json:/catty-reminders-app/config.json:ro
      # ВЫПОЛНЯЕМ ТРЕБОВАНИЕ МЕТОДИЧКИ: вечный изолированный том для хранения файлов приложения
      - catty-db-data:/catty-reminders-app/db/
    networks:
      - app-network

  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_USER: catty
      MYSQL_PASSWORD: cattypass
      MYSQL_DATABASE: catty_reminders
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -ucatty -pcattypass"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 20s
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - app-network

volumes:
  db-data:
  catty-db-data:

networks:
  app-network:
    driver: bridge
