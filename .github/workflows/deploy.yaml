name: CD Deploy
on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: course.prafdin.ru
          username: kali
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2200
          script: |
            cd ~/catty-reminders-app
            ./scripts/deploy.sh lab2 ${{ github.sha }}
