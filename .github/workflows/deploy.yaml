name: CD Pipeline
on:
  release:
    types: [published, created]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Execute deployment script
        run: |
          chmod +x ./deploy.sh
          ./deploy.sh ${{ github.sha }}
