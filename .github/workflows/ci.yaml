name: CI
on:
  push:
<<<<<<< HEAD
    branches: [ lab4* ]
  pull_request:
    branches: [ lab4* ]

=======
    branches:
      - '**'
>>>>>>> d8cc141 (test: trigger deploy pipeline)
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
<<<<<<< HEAD
      - name: Hadolint
        run: docker run --rm -i hadolint/hadolint hadolint - < Dockerfile

  pipeline:
    needs: docker-lint
    runs-on: ubuntu-latest
    services:
      db:
        image: mariadb:11.8.6-ubi9
        env:
          MARIADB_ROOT_PASSWORD: mypass
          MARIADB_DATABASE: catty_reminders
        ports: [ 3306:3306 ]

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with: { 
          python-version: "3.12" 
        }

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt uvicorn pytest-playwright
=======
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest-playwright
>>>>>>> d8cc141 (test: trigger deploy pipeline)
          playwright install --with-deps chromium
      - name: Build Docker image for testing
        run: |
<<<<<<< HEAD
          sed -i 's/"host": "db"/"host": "127.0.0.1"/g' config.json
      - name: Start app
        run: |
          nohup uvicorn app.main:app --host 127.0.0.1 --port 8181 &
          sleep 10

      - name: Run tests
        env: { 
          PYTHONPATH: "${{ github.workspace }}" 
        }
        run: pytest --junitxml=test_result.xml

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with: { 
          name: test_result, 
          path: test_result.xml 
        }
    
  docker-build:
    runs-on: ubuntu-latest
    needs: pipeline
    if: github.event_name == 'push'
    permissions: { 
      contents: read,
      packages: write 
    }
    steps:
      - uses: actions/checkout@v4
      - name: Login to GHCR
        uses: docker/login-action@v3
        with: { 
          registry: ghcr.io, 
          username: "${{ github.actor }}", 
          password: "${{ secrets.GITHUB_TOKEN }}" 
        }
        
      - name: Convert owner to lowercase
        run: |
          OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "OWNER_LOWER=$OWNER_LOWER" >> $GITHUB_ENV

=======
          docker build -t catty-test .
      - name: Run MySQL container
        run: |
          docker run -d \
            --name mysql-test \
            -e MYSQL_ROOT_PASSWORD=rootpass \
            -e MYSQL_USER=catty \
            -e MYSQL_PASSWORD=cattypass \
            -e MYSQL_DATABASE=catty_reminders \
            -p 3306:3306 \
            mysql:8.0
          sleep 15
      - name: Run app container
        run: |
          docker run -d \
            --name app-test \
            --link mysql-test:db \
            -e MYSQL_HOST=mysql-test \
            -e MYSQL_USER=catty \
            -e MYSQL_PASSWORD=cattypass \
            -e MYSQL_DATABASE=catty_reminders \
            -p 8181:8181 \
            catty-test
          sleep 5
      - name: Run tests
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          pytest --junitxml=test_result.xml -k "unit"
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test_result
          path: test_result.xml
  docker-build:
    runs-on: ubuntu-latest
    needs: test
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
>>>>>>> d8cc141 (test: trigger deploy pipeline)
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/vadimkadkdoofjf/catty-reminders-app:${{ github.sha }}
          build-args: |
            DEPLOY_REF=${{ github.sha }}
